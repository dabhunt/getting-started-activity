{
  "dependencies": {
    "@discord/embedded-app-sdk": "^2.2.0",
    "cloudflared": "^0.7.1"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "scripts": {
    "launch": "concurrently -k -n \"Vite\",\"Tunnel\" -c blue,magenta \"npm --prefix ./client run dev\" \"cloudflared tunnel --url http://localhost:5173\""
  }
}
